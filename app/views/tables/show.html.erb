<h1>Where the Tacho-Ritas Sit</h1>

<!-- This is a link to add a new guest to the specific table -->
<%= form_for @guest do |f| %>
  <%= f.hidden_field :table_id, value: @table.id %>
  <%= f.submit %>
<% end %>
<!--  Link to destroy all the guests with this specific table ID -->


<% @guests.each_with_index do |guest,index| %>
  <!-- This is a link to check to see the orders this guest has made -->
  <div class='tachorita'>
    <%= button_to "TACHO-RITA #{index+1}", guest_path(guest.id), method: :get, class:'name'%> <%= button_to 'X', guest_path(guest.id), method: :delete %>
  </div>

<% end %>

<!-- Need to be able to define the table_id for each new guest as this current table's id -->
